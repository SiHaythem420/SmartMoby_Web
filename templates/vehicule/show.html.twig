<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Vehicle Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <div class="row mb-4">
            <div class="col-12">
                <div class="btn-group" role="group">
                    <a href="{{ path('vehicule_index') }}" class="btn btn-primary active">
                        <i class="fa fa-car"></i> Vehicles
                    </a>
                    <a href="{{ path('trajet_index') }}" class="btn btn-outline-primary">
                        <i class="fa fa-route"></i> Trips
                    </a>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Vehicle Details</h3>
                    </div>
                    <div class="card-body">
                        <table class="table table-bordered">
                            <tbody>
                                <tr>
                                    <th>ID</th>
                                    <td>{{ vehicule.id }}</td>
                                </tr>
                                <tr>
                                    <th>Type</th>
                                    <td>{{ vehicule.type }}</td>
                                </tr>
                                <tr>
                                    <th>Capacity</th>
                                    <td>{{ vehicule.capacite }}</td>
                                </tr>
                                <tr>
                                    <th>Status</th>
                                    <td>{{ vehicule.statut }}</td>
                                </tr>
                                <tr>
                                    <th>Available</th>
                                    <td>{{ vehicule.dispo ? 'Yes' : 'No' }}</td>
                                </tr>
                            </tbody>
                        </table>
                        <div class="mt-3">
                            <a href="{{ path('vehicule_edit', {'id': vehicule.id}) }}" class="btn btn-warning">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <a href="{{ path('vehicule_index') }}" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> Back to list
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 